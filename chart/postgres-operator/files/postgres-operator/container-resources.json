            "resources": {
                "requests": {
                    "memory": "{{.RequestsMemory}}",
                    "cpu": "{{.RequestsCPU}}"
                },
                "limits": {
                    "memory": "{{.LimitsMemory}}",
                    "cpu": "{{.LimitsCPU}}"
                }
            },
