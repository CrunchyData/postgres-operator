---
# Annotate the cluster to trigger a backup.
apiVersion: postgres-operator.crunchydata.com/${KUTTL_PGCLUSTER_API_VERSION}
kind: PostgresCluster
metadata:
  name: otel-cluster
  annotations:
    postgres-operator.crunchydata.com/pgbackrest-backup: do-it
