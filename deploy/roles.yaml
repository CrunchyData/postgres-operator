kind: Role
apiVersion: rbac.authorization.k8s.io/v1beta1
metadata:
  name: pgo-role
  namespace: $PGO_OPERATOR_NAMESPACE
rules:
  - verbs:
      - '*'
    apiGroups:
      - '*'
    resources:
      - secrets
