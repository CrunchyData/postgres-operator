# Changes to this file should be made in hack/release-templates/config-singlenamespace-kustomization.yaml.
namespace: postgres-operator

commonLabels:
  postgres-operator.crunchydata.com/control-plane: postgres-operator

bases:
- ../crd
- ../rbac/namespace
- ../manager

patches:
- manager-target.yaml

images:
- name: postgres-operator
  newName: registry.developers.crunchydata.com/crunchydata/postgres-operator
  newTag: {{ubi8_pgo_latest_image_tag}}
