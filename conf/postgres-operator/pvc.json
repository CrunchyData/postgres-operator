{
    "kind": "PersistentVolumeClaim",
    "apiVersion": "v1",
    "metadata": {
        "name": "{{.PVCName}}",
        "labels": {
            "pgremove": "true"
        }
    },
    "spec": {
        "accessModes": [
            "{{.PVCAccessMode}}"
        ],
        "resources": {
            "requests": {
                "storage": "{{.PVCSize}}"
            }
        }
    }
}
