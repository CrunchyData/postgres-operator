FROM centos:7

LABEL Release="1.3.2" Vendor="Crunchy Data Solutions" 

RUN yum -y update && yum -y install hostname postgresql && yum -y clean all

ADD bin/postgres-operator /usr/local/bin

VOLUME ["/pgconf"]

USER root

ENTRYPOINT ["postgres-operator"]
