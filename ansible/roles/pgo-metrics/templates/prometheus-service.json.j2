{
    "kind": "Service",
    "apiVersion": "v1",
    "metadata": {
        "name": "{{ prometheus_service_name }}",
        "labels": {
            "name": "{{ prometheus_service_name }}",
            "vendor": "crunchydata"
        }
    },
    "spec": {
        "ports": [
            {
                "name": "prometheus",
                "protocol": "TCP",
                "port": {{ prometheus_port }},
                "targetPort": {{ prometheus_port }}
            }
        ],
        "selector": {
            "name": "{{ prometheus_service_name }}"
        },
        "type": "ClusterIP",
        "sessionAffinity": "None"
    }
}
