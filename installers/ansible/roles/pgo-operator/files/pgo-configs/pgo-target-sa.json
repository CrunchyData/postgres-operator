{
    "apiVersion": "v1",
    "kind": "ServiceAccount",
    "metadata": {
        "name": "pgo-target",
        "namespace": "{{.TargetNamespace}}",
        "labels": {
          "vendor": "crunchydata"
        }
    }
}
